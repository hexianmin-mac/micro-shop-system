<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为IUserDao接口方法提供sql语句配置 -->
<mapper namespace="org.hxm.microsys.core.dao.UserDao">

    <select id="findUserById" resultType="org.hxm.microsys.core.entity.User" parameterType="int">
        SELECT * FROM user WHERE userId = #{id}
    </select>


    <select id="login" resultType="org.hxm.microsys.core.entity.SysUser">
        select * from sys_user where sysUName = #{name} and password=#{password}
    </select>


    <select id="getAllUser" resultType="org.hxm.microsys.core.entity.User">
        select * from user order by userId asc
    </select>


</mapper>